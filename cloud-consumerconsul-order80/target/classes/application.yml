server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/atguigu?serverTimezone=UTC
    username: root
    password: 1234
  application:
    name: cloud-consumer-order
    ##consul注册中心地址
  cloud:
    consul:
      host: localhost
      discovery:
        #hostname: 127.0.0.1
        service-name: ${spring.application.name}
      port: 8500
